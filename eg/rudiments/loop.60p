// Include `support/${PLATFORM}.60p` before this source
// Should print ABCDEFGHIJKLMNOPQRSTUVWXYZ

define main routine
  trashes a, y, z, n, c
{
    ld y, 65
    repeat {
        ld a, y
        call chrout
        inc y
        cmp y, 91
    } until z
}
