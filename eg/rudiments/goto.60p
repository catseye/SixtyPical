define chrout routine
  inputs a
  trashes a
  @ 65490

define bar routine trashes a, z, n {
    ld a, 66
    call chrout
}

define main routine trashes a, z, n {
    ld a, 65
    call chrout
    goto bar
}
