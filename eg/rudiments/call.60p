// Include `support/${PLATFORM}.60p` before this source
// Should print AA

define print routine
  trashes a, z, n
{
    ld a, 65
    call chrout
}

define main routine
  trashes a, z, n
{
    call print
    call print
}
